defmodule FidelityRuleEngineTest do
  # use ExUnit.Case
  # doctest FidelityRuleEngine

  # test "greets the world" do
  #   assert FidelityRuleEngine.hello() == :world
  # end

  # test "test message" do
  #   ref = Broadway.test_message(FidelityRuleEngine, [1])
  #   assert_receive {:ack, ^ref, [%{data: 1}], []}
  #   Process.sleep(1000)
  #   IO.inspect(ref, label: "Reference")
  # end

end
